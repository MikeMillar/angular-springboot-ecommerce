import { Lub2ShopValidators } from './lub2-shop-validators';

describe('Lub2ShopValidators', () => {
  it('should create an instance', () => {
    expect(new Lub2ShopValidators()).toBeTruthy();
  });
});
